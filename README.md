# ISL Sign Language Translator

On-device Android app that recognizes Indian Sign Language (ISL) word signs
from the camera and shows them as text. Camera frames never leave the phone.

🚧 Work in progress. See [`training/`](training/) for the model pipeline.
